"use client";

import LoginForm from "@/component/LoginForm";
import { NextPage } from "next";

interface Props {}

const Page: NextPage<Props> = ({}) => {
  return <LoginForm />;
};

export default Page;
